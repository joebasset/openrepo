export default function App() {
  return (
    <main className="shell">
      <section className="card">
        <p className="eyebrow">Openrepo</p>
        <h1>{{ .ProjectName }}</h1>
        <p className="lede">
          Minimal React baseline with Vite, Vitest, and a small starter surface.
        </p>
      </section>
    </main>
  );
}
